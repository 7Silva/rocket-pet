pre-commit:
  commands:
    check:
      run: bunx format {staged_files}
      glob: '*.{js,jsx,ts,tsx,.mjs}'
      stage_fixed: true

pre-push:
  commands:
    validate:
      glob: '*.{js,jsx,ts,tsx,.mjs}'
      run: bunx validate:typecheck
